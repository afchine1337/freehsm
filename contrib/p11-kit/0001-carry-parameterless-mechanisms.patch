--- a/common/pkcs11.h
+++ b/common/pkcs11.h
@@ -744,6 +744,31 @@
 #define CKM_TLS12_KEY_SAFE_DERIVE	(0x3e3UL)
 #define CKM_TLS_MAC			(0x3e4UL)
 #define CKM_TLS_KDF			(0x3e5UL)
+/* PKCS#11 v3.0 SHA-3 and v3.2 post-quantum mechanisms. Present in the
+ * standard headers; absent here, which is why nothing could name them. */
+#define CKM_SHA3_256                   0x000002B0UL
+#define CKM_SHA3_256_HMAC              0x000002B1UL
+#define CKM_SHA3_256_HMAC_GENERAL      0x000002B2UL
+#define CKM_SHA3_256_KEY_GEN           0x000002B3UL
+#define CKM_SHA3_224                   0x000002B5UL
+#define CKM_SHA3_224_HMAC              0x000002B6UL
+#define CKM_SHA3_224_HMAC_GENERAL      0x000002B7UL
+#define CKM_SHA3_224_KEY_GEN           0x000002B8UL
+#define CKM_SHA3_384                   0x000002C0UL
+#define CKM_SHA3_384_HMAC              0x000002C1UL
+#define CKM_SHA3_384_HMAC_GENERAL      0x000002C2UL
+#define CKM_SHA3_384_KEY_GEN           0x000002C3UL
+#define CKM_SHA3_512                   0x000002D0UL
+#define CKM_SHA3_512_HMAC              0x000002D1UL
+#define CKM_SHA3_512_HMAC_GENERAL      0x000002D2UL
+#define CKM_SHA3_512_KEY_GEN           0x000002D3UL
+#define CKM_ML_KEM_KEY_PAIR_GEN        0x0000000FUL
+#define CKM_ML_KEM                     0x00000017UL
+#define CKM_ML_DSA_KEY_PAIR_GEN        0x0000001CUL
+#define CKM_ML_DSA                     0x0000001DUL
+#define CKM_SLH_DSA_KEY_PAIR_GEN       0x0000002DUL
+#define CKM_SLH_DSA                    0x0000002EUL
+
 #define CKM_KEY_WRAP_LYNKS		(0x400UL)
 #define CKM_KEY_WRAP_SET_OAEP		(0x401UL)
 #define CKM_CMS_SIG			(0x500UL)
--- a/p11-kit/rpc-message.c
+++ b/p11-kit/rpc-message.c
@@ -1540,6 +1540,33 @@
 	case CKM_RIPEMD160:
 	case CKM_RIPEMD160_HMAC:
 	case CKM_KEY_WRAP_LYNKS:
+	/* SHA-3, and the post-quantum key-pair generators. Every one of these
+	 * takes no parameter in any mode, so serialising it is the empty byte
+	 * array and there is nothing to get wrong.
+	 *
+	 * Deliberately NOT here: CKM_ML_DSA, CKM_SLH_DSA and the CKM_HASH_*
+	 * family. Those take an OPTIONAL CK_SIGN_ADDITIONAL_CONTEXT, and
+	 * listing them as parameterless would serialise a supplied context as
+	 * absent -- the server would sign with an empty context and return a
+	 * signature that verifies against nothing the caller expects. A wrong
+	 * signature is a worse outcome than a refused one. They are carried by
+	 * the parameter-aware test below instead, which admits the call only
+	 * when the caller supplied no context. */
+	case CKM_SHA3_224:
+	case CKM_SHA3_224_HMAC:
+	case CKM_SHA3_224_KEY_GEN:
+	case CKM_SHA3_256:
+	case CKM_SHA3_256_HMAC:
+	case CKM_SHA3_256_KEY_GEN:
+	case CKM_SHA3_384:
+	case CKM_SHA3_384_HMAC:
+	case CKM_SHA3_384_KEY_GEN:
+	case CKM_SHA3_512:
+	case CKM_SHA3_512_HMAC:
+	case CKM_SHA3_512_KEY_GEN:
+	case CKM_ML_DSA_KEY_PAIR_GEN:
+	case CKM_ML_KEM_KEY_PAIR_GEN:
+	case CKM_SLH_DSA_KEY_PAIR_GEN:
 		return true;
 	default:
 		return false;
@@ -1555,6 +1582,33 @@
 	return false;
 }
 
+/*
+ * Whether a particular CALL can be serialised, which is not the same question
+ * as whether the mechanism type is known.
+ *
+ * The allow-list exists because a CK_MECHANISM parameter is a void* whose
+ * layout depends on the mechanism, so an unrecognised type cannot be encoded
+ * safely. That reasoning does not apply when the caller supplied no parameter
+ * at all: the wire form is the empty byte array, identical to every mechanism
+ * in mechanism_has_no_parameters(), and there is nothing left to misencode.
+ *
+ * This admits every parameterless use of a mechanism p11-kit does not know --
+ * vendor mechanisms included -- while still refusing the case the allow-list
+ * was built to refuse, which is a parameter it cannot describe. It is also
+ * safer than adding a mechanism with an optional parameter to the list: if a
+ * caller does supply a context to CKM_ML_DSA, this returns false and the call
+ * is refused, rather than the context being silently dropped.
+ */
+bool
+p11_rpc_mechanism_call_is_supported (const CK_MECHANISM *mech)
+{
+	if (mech == NULL)
+		return false;
+	if (p11_rpc_mechanism_is_supported (mech->mechanism))
+		return true;
+	return mech->pParameter == NULL && mech->ulParameterLen == 0;
+}
+
 void
 p11_rpc_buffer_add_mechanism (p11_buffer *buffer, const CK_MECHANISM *mech)
 {
@@ -1568,6 +1622,23 @@
 		p11_rpc_buffer_add_byte_array (buffer, NULL, 0);
 		return;
 	}
+
+	/* A type this file does not recognise reached here only because the
+	 * caller supplied no parameter -- see p11_rpc_mechanism_call_is_supported.
+	 * The empty byte array is the whole of it, and p11_rpc_buffer_get_mechanism
+	 * reads it back with the generic byte-array serialiser, so the two agree.
+	 *
+	 * This test MUST come after mechanism_has_sane_parameters and not before
+	 * it. A mechanism that HAS a registered serialiser still has to go through
+	 * that serialiser even when this particular call passes NULL, because the
+	 * decoder looks the serialiser up by type and cannot know the encoder took
+	 * a short cut. Testing the parameter first desynchronises the stream: it
+	 * passes p11-kit 0.24.0's suite and fails master's test-transport, where
+	 * every case after the first dies inside setup_mock_module. */
+	if (!mechanism_has_sane_parameters (mech->mechanism)) {
+		p11_rpc_buffer_add_byte_array (buffer, NULL, 0);
+		return;
+	}
 
 	assert (mechanism_has_sane_parameters (mech->mechanism));
 
--- a/p11-kit/rpc-message.h
+++ b/p11-kit/rpc-message.h
@@ -450,6 +450,8 @@
 
 bool             p11_rpc_mechanism_is_supported          (CK_MECHANISM_TYPE mech);
 
+bool             p11_rpc_mechanism_call_is_supported     (const CK_MECHANISM *mech);
+
 void             p11_rpc_buffer_add_mechanism            (p11_buffer *buffer,
 							  const CK_MECHANISM *mech);
 
--- a/p11-kit/rpc-client.c
+++ b/p11-kit/rpc-client.c
@@ -393,26 +393,29 @@
 	return p11_buffer_failed (msg->input) ? PARSE_ERROR : CKR_OK;
 }
 
+/*
+ * C_GetMechanismList used to be filtered down to the mechanisms this file
+ * knows how to serialise. That made sense while the decision could be taken
+ * from the type alone. It cannot any more: whether a call can be carried now
+ * depends on whether the caller supplies a parameter, so a mechanism this file
+ * has never heard of may still be perfectly usable.
+ *
+ * Filtering therefore hid working mechanisms. Measured against a module
+ * offering post-quantum signatures: 72 mechanisms advertised, 20 reported
+ * through the socket, and the 52 hidden included every one the module existed
+ * to provide.
+ *
+ * Reporting them and letting the per-call test refuse what it must is both
+ * more useful and more honest: a mechanism that is advertised and then fails
+ * at C_*Init is ordinary PKCS#11, whereas a mechanism that is advertised by
+ * the module and invisible through the proxy is a lie about the token.
+ */
 static void
 mechanism_list_purge (CK_MECHANISM_TYPE_PTR mechs,
                       CK_ULONG *n_mechs)
 {
-	int i;
-
 	assert (mechs != NULL);
 	assert (n_mechs != NULL);
-
-	for (i = 0; i < (int)(*n_mechs); ++i) {
-		if (!p11_rpc_mechanism_is_supported (mechs[i])) {
-
-			/* Remove the mechanism from the list */
-			memmove (&mechs[i], &mechs[i + 1],
-			         (*n_mechs - i) * sizeof (CK_MECHANISM_TYPE));
-
-			--(*n_mechs);
-			--i;
-		}
-	}
 }
 
 static CK_RV
@@ -426,7 +429,7 @@
 	/* Make sure this is in the right order */
 	assert (!msg->signature || p11_rpc_message_verify_part (msg, "M"));
 
-	if (!p11_rpc_mechanism_is_supported (mech->mechanism))
+	if (!p11_rpc_mechanism_call_is_supported (mech))
 		return CKR_MECHANISM_INVALID;
 
 	/*
@@ -616,9 +619,11 @@
 	if (!p11_rpc_message_write_attribute_array (&_msg, (arr), (num))) \
 		{ _ret = CKR_HOST_MEMORY; goto _cleanup; }
 
+/* No serialisation of a parameter happens here -- this carries a bare
+ * mechanism type, for C_GetMechanismInfo and friends. Refusing types this
+ * file does not recognise meant the proxy would not report information the
+ * module was perfectly willing to give. */
 #define IN_MECHANISM_TYPE(val) \
-	if(!p11_rpc_mechanism_is_supported (val)) \
-		{ _ret = CKR_MECHANISM_INVALID; goto _cleanup; } \
 	if (!p11_rpc_message_write_ulong (&_msg, val)) \
 		{ _ret = CKR_HOST_MEMORY; goto _cleanup; }
 
