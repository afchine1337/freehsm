# Reference (branch, tag, or commit SHA) of github.com/C2SP/wycheproof
# to fetch from. For maximum reproducibility put a 40-char hex commit
# SHA here ; for bootstrapping or quick experimentation a branch name
# like 'main' is acceptable.
#
# How to upgrade from 'main' to a pinned SHA (after first successful run) :
#   1. cd tests/wycheproof
#   2. git ls-remote https://github.com/C2SP/wycheproof.git HEAD
#   3. Copy the SHA from the first column into VECTORS_SHA (no newline)
#   4. ./fetch_vectors.sh   # downloads the pinned tree
#   5. ./run_wycheproof.py  # confirm no regression
#   6. git commit -s -m "tests(wycheproof): pin VECTORS_SHA to <short>"
#
# Last bumped : 2026-06-13 (initial bootstrap with branch 'main')
main
